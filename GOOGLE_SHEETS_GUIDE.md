# 🔗 วิธีใช้ Google Sheets แทนไฟล์ CSV

ด้วยวิธีนี้ คุณสามารถ:
- ✅ ดึงข้อมูลจาก Google Sheets โดยตรง
- ✅ อัปเดตข้อมูลแบบ Real-time โดยไม่ต้องแก้ไขไฟล์
- ✅ แชร์ข้อมูลกับทีมของคุณได้ง่าย

---

## ⚙️ ขั้นตอนที่ 1: เตรียม Google Sheet 📋

### 1.1 ตรวจสอบโครงสร้าง Sheet

Google Sheet ของคุณต้องมีคอลัมน์ดังต่อไปนี้ (ในแถวแรก):

| Type | Text | Start Date | End Date |
|:---|:---|:---|:---|
| HK | ประชุมแผน | 2025-01-01 | 2025-01-01 |
| IT | อัปเดตระบบ | 2025-01-10 | 2025-01-15 |
| APP | ออกแบบ Mockup | 2025-01-20 | 2025-02-05 |

**ตรวจสอบให้แน่ใจว่า:**
- ✅ คอลัมน์แรกชื่อ **Type** (ประเภท/ฝ่ายงาน เช่น HK, IT, APP, PAS, HT_Report)
- ✅ คอลัมน์ที่สองชื่อ **Text** (รายละเอียดโครงการ)
- ✅ คอลัมน์ที่สามชื่อ **Start Date** (วันเริ่มต้น - รูปแบบ YYYY-MM-DD หรือ YYYY/MM/DD)
- ✅ คอลัมน์ที่สี่ชื่อ **End Date** (วันสิ้นสุด - รูปแบบ YYYY-MM-DD หรือ YYYY/MM/DD)

---

## 📤 ขั้นตอนที่ 2: เผยแพร่ Google Sheet ไปยังเว็บ

### 2.1 ไปที่ Google Sheet ของคุณ
- เปิด Google Sheet ที่มีข้อมูลของคุณ

### 2.2 คลิก "Share" (แชร์)
1. คลิกปุ่ม **"Share"** ที่มุมบนขวา

### 2.3 หา "Publish to the web"
1. ค้นหา **"Publish to the web"** ในตัวเลือกที่ปรากฏ
2. คลิกเพื่อเปิด

### 2.4 ตั้งค่าการเผยแพร่
1. **เลือก Sheet** ที่มีข้อมูลของคุณ (ถ้ามี Sheet หลายตัว)
2. **เลือกรูปแบบ:** Comma-separated values (.csv)
3. คลิก **"Publish"**

### 2.5 คัดลอก URL
- URL ที่ปรากฏจะมีลักษณะเช่นนี้:
```
https://docs.google.com/spreadsheets/d/1xxxxxxxxxxx/export?format=csv&gid=0
```
- **คัดลอก URL นี้ไว้** (ใช้ปุ่ม Copy)

---

## 🔧 ขั้นตอนที่ 3: แก้ไขโค้ด HTML

### 3.1 เปิดไฟล์ HTML
- เปิดไฟล์ `2.html` หรือ `3.html` ด้วย Text Editor (เช่น VS Code)

### 3.2 หาส่วน JavaScript
ค้นหา (Ctrl + F) ฟังก์ชัน `autoLoadCSV()` - คุณจะเห็นโค้ดแบบนี้:

```javascript
function autoLoadCSV() {
    fetch('data.csv')
        .then(r => r.text())
        .then(text => loadFromCsvText(text))
        .catch(err => {
            console.log('ไม่พบไฟล์ data.csv หรือเกิดข้อผิดพลาด:', err);
            $('#monthsView').html('<div class="text-center p-5 text-danger" style="grid-column:1/-1;">ไม่พบไฟล์ data.csv ในโฟลเดอร์นี้</div>');
        });
}
```

### 3.3 แทนที่ URL
เปลี่ยน `'data.csv'` เป็น URL ที่คุณคัดลอกมา:

```javascript
function autoLoadCSV() {
    fetch('https://docs.google.com/spreadsheets/d/1xxxxxxxxxxx/export?format=csv&gid=0')
        .then(r => r.text())
        .then(text => loadFromCsvText(text))
        .catch(err => {
            console.log('ไม่สามารถโหลดข้อมูลจาก Google Sheets:', err);
            $('#monthsView').html('<div class="text-center p-5 text-danger" style="grid-column:1/-1;">เกิดข้อผิดพลาดในการดึงข้อมูล: ' + err.message + '</div>');
        });
}
```

**สิ่งที่ต้องเปลี่ยน:**
- ❌ เปลี่ยนจาก: `'data.csv'`
- ✅ เปลี่ยนเป็น: `'https://docs.google.com/spreadsheets/d/1xxxxxxxxxxx/export?format=csv&gid=0'`

### 3.4 บันทึกไฟล์
- ใช้ **Ctrl + S** (หรือ Cmd + S บน Mac)

---

## 🧪 ขั้นตอนที่ 4: ทดสอบ

### 4.1 เปิดไฟล์ HTML ในเบราว์เซอร์
- ดับเบิลคลิกที่ไฟล์ `2.html` หรือ `3.html`
- หรือคลิกขวา → "Open with" → เลือกเบราว์เซอร์

### 4.2 ตรวจสอบว่าข้อมูลโหลดหรือไม่
- **✅ สำเร็จ:** คุณจะเห็นข้อมูลจากการ์ดเดือนแสดง
- **❌ ล้มเหลว:** ตรวจสอบ Console (F12 → Console) เพื่อดูข้อความแสดงข้อผิดพลาด

---

## 🐛 แก้ไขปัญหา

### ❌ ปัญหา: "ไม่มีข้อมูลแสดงผล"

**สาเหตุที่เป็นไปได้:**
1. ชื่อคอลัมน์ไม่ตรงกัน
2. รูปแบบวันที่ผิด
3. URL ไม่ถูกต้อง

**วิธีแก้:**
1. ตรวจสอบชื่อคอลัมน์ใหม่: **Type, Text, Start Date, End Date**
2. ตรวจสอบรูปแบบวันที่: ต้องเป็น `YYYY-MM-DD` หรือ `YYYY/MM/DD`
3. เปิด Browser Console (F12) และดูข้อผิดพลาด
4. ยังไม่สำเร็จ? ลองทดสอบ URL ด้วยการวางไปในเบราว์เซอร์ ควรแสดง CSV ในรูปแบบข้อความ

### ❌ ปัญหา: "Error 404" หรือ "Network Error"

**สาเหตุ:** URL ไม่ถูกต้องหรือ Sheet ยังไม่ได้เผยแพร่

**วิธีแก้:**
1. ไปกลับไป Google Sheet
2. ตรวจสอบว่า "Publish to the web" ถูกตั้งค่าแล้ว
3. คัดลอก URL ใหม่
4. วางแนว URL ใหม่ลงในโค้ด

### ❌ ปัญหา: "CORS Error"

บางครั้ง Google Sheets อาจมีข้อ จำกัดด้าน CORS

**วิธีแก้:**
1. ลองใช้ `https://cors-anywhere.herokuapp.com/` เป็น proxy (แม้ว่าไม่แนะนำ)
2. หรือใช้ Google Sheets API แทน (ต้องมีความรู้เพิ่มเติม)

---

## 💡 เคล็ดลับ

### 📝 อัปเดตข้อมูล
- **เพียงแก้ไข Google Sheet:** ข้อมูลจะอัปเดตทันทีเมื่อคุณรีเฟรชเบราว์เซอร์ (F5)
- ไม่ต้องแก้ไขไฟล์ HTML อีก

### 👥 แชร์กับทีม
- คุณสามารถแชร์ Google Sheet กับสมาชิกทีมเพื่อให้พวกเขาแก้ไขข้อมูลได้
- ข้อมูลจะอัปเดตอัตโนมัติสำหรับทุกคนที่เปิดรายงาน

### 📊 ใช้หลาย Sheet
- หากต้องการใช้ Sheet ต่างกัน ให้เปลี่ยน `gid=0` ในส่วนท้ายของ URL
- `gid=0` = Sheet แรก
- `gid=123456` = Sheet อื่น (ต้องแก้ ID ตามต้องการ)

---

## 🔒 ความปลอดภัย

⚠️ **สำคัญ:** เมื่อคุณ "Publish to the web" Google Sheet จะเป็น **Public**
- ❌ อย่าใส่ข้อมูลที่เป็นความลับ (เช่น รหัสผ่าน, ข้อมูลส่วนตัว)
- ✅ ใส่เฉพาะข้อมูลโครงการที่สามารถแชร์ได้

**หากต้องการเปลี่ยน:**
1. ไป Google Sheet Settings
2. เปลี่ยนเป็น "Restricted" เพื่อให้เฉพาะคนที่ได้รับสิทธิ์เท่านั้นเข้าถึง

---

## 📞 ติดต่อ

หากมีปัญหา:
1. ตรวจสอบ [GitHub Issues](https://github.com/Doz555/TimeLineOfYearWorker)
2. ดู Browser Console (F12 → Console) เพื่อดูข้อความแสดงข้อผิดพลาด
3. สร้าง Issue ใน GitHub พร้อมข้อมูลรายละเอียด

---

**สร้างเมื่อ:** December 2025
**เวอร์ชัน:** 1.0
